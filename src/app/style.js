import { makeStyles } from '@material-ui/core';
const useStyles = makeStyles((theme) => ({
    grid: {
        width: '100%',
        margin: '0px',
     
    },
    typo:{
        color: 'black',
        fontWeight: '700',
        marginBottom: '50px',
        marginTop: '80px',
        
    }
}));
export default useStyles;